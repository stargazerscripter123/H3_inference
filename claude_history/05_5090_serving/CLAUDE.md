# 主题: 5090 双卡 vLLM / SGLang serving

范围: 2×5090(32G)上两栈 serving 的可行性验证 / h3_switch_5090.sh / CLI 扩展。
状态: **已结案(2026-08-08)—— 不可行**,瓶颈是主机内存(125G,官方配方要求 384G 级)。
根因表与恢复条件见 FINAL.md;完整过程见 session notes。
不要在未加内存/无 swap 的情况下重试同样的配置 —— 四种组合的失败根因均为资源数学,非配置可调。
环境(权重/env/脚本)保留完好,满足恢复条件后 `h3_switch_5090.sh vllm|sglang` 直接可用。
