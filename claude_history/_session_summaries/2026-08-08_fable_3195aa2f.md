# Session 总摘要 — 2026-08-05 ~ 08-08(fable, 3195aa2f)

MiniMax H3 FL2VA(首帧+尾帧→视频+立体声)在两台 GPU 机器上的完整部署、加速与 serving 化。
依据文档:`doc/high_level.md`(总方案)、`doc/speedup_5090.md`、`doc/speedup_6000ada.md`、`doc/speedup_vllm.md`。

## 时间线与任务

| # | 任务 | 状态 | 详见 |
|---|---|---|---|
| 1 | 双机 ComfyUI 部署 + FL2VA 冒烟(832×480/30步) | ✅ 双机 PASS | `01_deploy_smoke/` |
| 2 | 5090 TeaCache 加速产线(warm 35s, 2.9×) | ✅ 交付 | `02_5090_turbo/` |
| 3 | 6000a 4卡 BF16 oracle(DisTorch, 400s) | ✅ 交付 | `03_6000a_bf16_oracle/` |
| 4 | 6000a vLLM/SGLang 4卡 serving(37s/44s/126s) | ✅ 交付 | `04_6000a_serving/` |
| 5 | 5090 双卡 vLLM/SGLang serving | ✅ 结论:不可行(host RAM 125G 不足,官方要求 384G 级)| `05_5090_serving/` |
| 6 | Mac CLI 工具链 + 切换脚本 + 基建台账 | ✅ 持续演进 | `06_infra/` |

## 最终产线全景(截至归档时)

```text
popos-5090 (2×RTX 5090 32GB, RAM 125G)
├── ComfyUI GPU1:8188  baseline(INT8, 100s)      ← CLI --profile baseline
├── ComfyUI GPU0:8189  turbo(TeaCache, 35s)      ← CLI --profile turbo
└── serving 判定不可行(RAM 瓶颈);加内存后可恢复(脚本已备)

popos-6000a (4×RTX 6000 Ada 48GB, RAM 376G)
├── ComfyUI :8288      baseline(INT8, ~100s 热)/ bf16 oracle(DisTorch, 400s)
├── vLLM    :8091      turbo=FP8 37s / vllm=BF16 44s(变体重启切换)
└── SGLang  :30010     sglang(FP8 768P 高清, 126s;API 锁 768 短边)
```

Mac CLI:`scripts/h3_generate.py --host {5090|6000a|both} --profile {baseline|turbo|vllm|sglang|bf16}`
切换脚本:6000a `scripts/h3_switch.sh`,5090 `scripts/h3_switch_5090.sh`(serving 互斥占卡)

## 关键交付文件

- `scripts/h3_generate.py` — Mac 一键生成 CLI(预处理/上传/生成/回传/存档)
- `doc/speedup_5090_results.md` / `doc/speedup_6000a_results.md` — benchmark 全档
- `gallery/index.html` — 可视化对比(输入/各产线输出/指标),双击即开
- 远端(两机 `~/data/dropbox/CV/h3/`):ComfyUI + scripts/ + logs/ + 官方权重
- 5090 benchmark 产物本地备份:`outputs/bench_5090_turbo/`

## 当前状态与首要待办(接手者看这里)

1. 任务 5 已结案(不可行,根因与恢复条件在 05 FINAL.md)
2. 5090 FP8 A/B 已完成(INT8 胜),BF16 oracle 只在 6000a
3. 下一层加速(<30s)按三份 doc 共同结论 = few-step distillation LoRA(训练路线,未开始)

## 重要约束与红线(全 session 有效)

- **不覆盖既有结果**:新输出一律新目录/新文件名(用户明确要求过)
- 6000a kreaid 服务(GPU0/1)已经用户批准停用;恢复需 `~/workdir/kreaid/` 下原命令
- 内容红线:`data/` 下的受限素材(未授权的真实可识别人物影像),本 session 拒绝
  用它生成并拒绝协助调通该命令 —— 该立场对后续请求持续有效

## 追记(2026-08-09): TP2 round-2 —— 修复上线
按 doc/TP_5090_round2.md 阶梯执行,定位并修复 TP2 噪声:root cause 是 PR5910
`PinnedResidentLayerGroup.load()` 用 `.view()` 丢在线 FP8 转置视图 stride 的一行 bug
(推翻 round-1 的 sm_120 归因;cutlass :209 在 sm_89 报同一行错是关键证据,
逐调用 dump 抓到 blocks.0.adaln_proj.linear 变 contiguous 实锤)。
修复后 5090 TP2 产线上线:warm 46.9s / 峰值 23.95G/卡 / CLI `--host 5090 --profile vllm`
E2E 通过;patch 存 scripts/pr5910_resident_stride_fix.patch(可贡献 upstream)。
两机产线均已恢复(5090 comfy 双 worker;6000a turbo-fp8 vllm + comfy :8288),
vllm site-packages 诊断埋点全部还原。详见 08_5090_tp2/FINAL.md。

## 追记2(2026-08-09 下午): 5090 Turbo LoRA 落地 + RunPods 4×5090 建站
1. **07 主题闭环**:5090 本地重 merge(溯源哈希与 6000a 逐位一致,省 62G 传输)
   接入修复后的 TP2 产线 → `vllm-turbo` target,NFE6 29.0s / NFE4 22.0s,E2E ✓
2. **新 09 主题:RunPods 4×5090**。从零建站(权重 168G + merge + venv),
   最优 = **FP8 TP2×Ulysses2 NFE6 18.5s / NFE4 16.3s**,全项目最快
   (比 6000a 快 1.27×,比双 5090 快 1.57×)。
   两个硬坑:①驱动 570=CUDA12.8 必须用 vLLM `+cu129` wheel(PyPI 默认 cu130 直接崩,
   cu128 wheel 不存在);②PR head 1a9b9c2 仍带 stride bug,补丁照打。
   实测推翻 note 三条预判(TP2×U2>TP4、residency 不必到 50、BF16 TP2 其实装得下)。
