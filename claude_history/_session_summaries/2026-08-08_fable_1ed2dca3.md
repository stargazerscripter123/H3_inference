# Session 总摘要 · 2026-08-08/09 · fable · 1ed2dca3

**主题**：07_turbo_lora（新建）——把 `larryvrh/MiniMax-H3-Turbo-Lora` 融入
6000a 的 vLLM-Omni 与 SGLang。计划经外部 research feedback 修正后执行
（NFE 语义、独立 merge oracle、四路径评测、backport 而非手写补丁）。

## 交付

- **vLLM merged FP8（`vllm-turbo fp8`）：NFE6 = 23.5s / NFE4 = 17.9s**
  （864×480，timed×3 离散 ≤0.1s；原 turbo 12 步 37.7s → 1.60×/2.11×；
  ComfyUI baseline → 4.3×）。<30s 目标达成。
- SGLang merged（`sglang-turbo`）：768P NFE6 = 72.1s（原 126.2s → 1.75×）；
  a2a 同分辨率两引擎打平。
- SGLang dynamic（`sglang-lora`，backport 4c28e24）：259/266 层命中、
  `/v1/set_lora` 热切 ckpt850 可用；VRAM 贴顶 → 研究工具定位。
- Mac CLI 新 profile `turbo-lora`/`sglang-turbo`（`--nfe`，默认 6）e2e 过；
  旧 profile 不动。
- merge 三层校验全过（位级 535/535、Comfy 单文件独立 oracle 逐位、前向
  cos≥0.999999）；merged 目录 +62G 带 manifest/.complete 契约。

## 关键修正（对 doc/turbo_lora.md）

audio shift 实为 12/3（shift6 属旧 ckpt500，ablation 证实无增益已弃）；推荐
ckpt=v4_step600_ema；键名引擎原生无需转换；真阻塞在 TP 切片（vLLM fused 跳过、
SGLang 上游 914644e8 已修）；engine steps=NFE+1 语义（三方 sigma 单测锁定）。

## 质量快筛（Stage A 8 case，帧级+YDIF 探针；90+ 单零失败）

7/8 case NFE6 全优；唯一边界=极端运动 fight（v4 NFE4 失败，ckpt850 NFE4 保
动态更好→大运动备选）；归因成立（base 同 NFE 运动前载后僵）。**Stage B 视频
盲评待用户**，通过后可把 turbo-lora 转正为默认 turbo。

## 档案

07_turbo_lora/{CLAUDE.md,FINAL.md,notes}；doc/turbo_lora_results.md；
06_infra FINAL 增 9-14 条（新 switch 模式/别名目录/backport 分支/NFE 口径）；
gallery 增 Turbo LoRA 区块；auto-memory 指针更新。
